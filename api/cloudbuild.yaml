steps:
  - name: "python"
    args: ["pip", "install", "-r", "requirements.txt"]

  - name: "gcr.io/cloud-builders/gcloud"
    args: ["app", "deploy", "--project=${secrets.PROJECT_ID}", "--app=${secrets.APP_ID}"]
